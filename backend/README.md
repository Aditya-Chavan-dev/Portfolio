# Portfolio Backend

## Setup
1. `npm install`
2. Create `.env` (needs `FIREBASE_SERVICE_ACCOUNT_KEY`).
3. `npm run dev`

## Deployment (Render)
- Connect repo to Render.
- Set Root Directory to `backend`.
- Add Environment Variables in Render Dashboard.
- Build Command: `npm install`
- Start Command: `node server.js`
